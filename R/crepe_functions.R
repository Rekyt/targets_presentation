# Functions --------------------------------------------------------------------
take_bowl <- function(x) {0}
add_flour <- add_sugar <- add_egg <- add_butter <- add_milk <- function(x, y) {
  x + y
}
whisk <- cook_in_pan <- function(x) x
